# CORE is the working set

CORE is not a MENU-only pin list. Opening a law, MENU `i`, and CLI query (`normen StVG`) all require the law to be on CORE. The Bundesrecht pane is how the working set grows. Pin-only CORE would have let scripts and `i` jump to any indexed law, but then MENU would lie about what the app can actually open.
